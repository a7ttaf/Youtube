# Phase 4 — S2 Stack → main Integration Runlog

**Date:** 2026-05-21
**Author:** Director DevOps / QA / Software Engineer
**Status:** In progress (prerequisite verification recorded; integration branch cut)
**Plan:** [`Docs/implementation/S2_INTEGRATION_PLAN.md`](../../implementation/S2_INTEGRATION_PLAN.md)
**Spec:** [`Docs/superpowers/specs/2026-05-21-s2-integration-and-recovery-design.md`](../specs/2026-05-21-s2-integration-and-recovery-design.md)
**Execution plan:** [`Docs/superpowers/plans/2026-05-21-s2-integration-and-recovery.md`](../plans/2026-05-21-s2-integration-and-recovery.md)

---

## 1. Prerequisite verification report (read-only, captured before branch cut)

### 1.1 origin/main SHA

- `origin/main` = `86c26e63e2f74eeb51bb68612f4a8e6e61d93b12` (PR #35 merge commit)
- Local `HEAD` aligned to the same SHA after fast-forward `0f5d318..86c26e6`.

### 1.2 S2 stack branch

- `origin/pr/s2-4a-tenant-id-on-operational-tables` head = `a637c8d0b08cf10d7284f9062dae7ceb6c427d84`
- 14 sibling `pr/s2-*` branches present on origin (`pr/s2-1-tenants-table`, `pr/s2-2-tenancy-package`, `pr/s2-3-principal-platform-admin`, `pr/s2-4a-tenant-id-on-operational-tables`, `pr/s2-4b-*` family).

### 1.3 alembic heads on origin/main (live)

```
20260513_0006 (head)
20260516_0001 (head)
```

Main is at 2 heads pre-integration. This matches the integration plan's "defensive (3-head)" pre-merge audit scenario rather than the documented "expected (2-head)" scenario. The plan's expectation that `20260516_0001` would be transitively included via `20260518_0001` only holds *after* the stack is merged in.

### 1.4 alembic heads on S2 stack (computed via `git show` revision/down_revision parsing, no checkout)

Chain at `origin/pr/s2-4a-tenant-id-on-operational-tables`:

```
20260512_0002 → 20260513_0001 → 20260516_0001 → 20260517_0001 → 20260518_0001
```

Single head: **`20260518_0001`**. Stack does not contain `20260513_0002..0006` (the retire_graph_permissions + 0513 finance chain is main-only).

### 1.5 Blob divergence on `20260516_0001_tenants_foundation.py`

| Ref | Blob SHA |
|---|---|
| `origin/main` | `25678b9aa23f216ed0915c0ae6116f00e9251869` |
| `origin/pr/s2-4a-tenant-id-on-operational-tables` | `078a1bb42d4884616f8f21f16cb0f46d6d35c9bd` (older) |
| `origin/pr/s2-4b-org-1-channel-registry` | `25678b9aa23f216ed0915c0ae6116f00e9251869` (matches main) |

S2.4a has the older blob. Main and downstream S2.4b PRs agree on the newer blob. **Initial conflict resolution: take main's `25678b9a` (explicit exception to plan's default `--theirs` rule for this single file).**

Post-validation correction: live diff showed the two blobs differ only by import ordering and one blank line. The final
file keeps the same migration semantics and uses Ruff-sorted imports so the integration does not introduce a new `I001`
gate failure.

### 1.6 Disposable PostgreSQL availability

- Docker 29.4.3 + Compose v5.1.4 available
- `postgres:18-alpine` pinned by digest in `docker-compose.yml`
- `.env.local` / `.env` absent (only `.env.example`); compose `:?` env-var enforcement would block plain `docker compose up postgres`
- **Resolution: standalone `docker run` (Option c) on non-default host port `55432`, repo config untouched.**

### 1.7 Runlog path

- `Docs/superpowers/runlog/2026-05-21-phase-4.md` (this file)
- Sibling: `Docs/superpowers/runlog/2026-05-21-phase-0.md`

### 1.8 Node build shell command

Bash-tool spawns non-login shells. System Nodist v20.10.0 wins PATH unless overridden. Required form:

```bash
source ~/.bashrc && cd frontend && npm install && npm run build
```

Verified post-source resolution: `node` → v22.14.0 from `~/.local/lib/node/node`, `npm` → 10.9.2 — both clear vite 8.0.13 floor (>=20.19 / >=22.12).

---

## 2. Operator decisions (2026-05-21)

1. **`20260516_0001_tenants_foundation.py` conflict resolution**: preserve main's migration semantics. Literal main blob `25678b9aa23f216ed0915c0ae6116f00e9251869` was initially forced, then normalized to Ruff-sorted import order after live diff proved the only divergence from the S2 blob was formatting. Explicit exception to plan default `--theirs` remains for any substantive migration content.
2. **Disposable PostgreSQL**: standalone `docker run` only. Do NOT create `.env.local`. Use non-default host port `55432` to avoid local conflicts. Record exact `DATABASE_URL` in this runlog when the container starts.
3. **Alembic validation path**: Option B (`downgrade base → upgrade head`) preferred. Acceptable fallback: Option A bouncing `LAST_PRE_MERGE_HEAD = 20260518_0001`.
4. **Branch name**: `pr/s2-integration-merge` (cut from `origin/main` at `86c26e6`).
5. **Merge type for the integration PR**: `gh pr merge --merge` (NOT squash, NOT rebase) per plan.
6. **Pause at every push/merge to remote** standing constraint applies through Phase 4.

---

## 3. Execution log (running)

### 3.1 Branch cut

- Source: `origin/main` @ `86c26e63e2f74eeb51bb68612f4a8e6e61d93b12`
- Target branch: `pr/s2-integration-merge`
- Command record: see "Execution timeline" below.

### 3.2 Stack merge

_(pending — paused per operator after branch cut)_

### 3.3 Conflict resolution

_(pending)_

### 3.4 Alembic merge migration generation + no-op verification

_(pending)_

### 3.5 Disposable PostgreSQL validation

- Planned DATABASE_URL: `postgresql+psycopg://ums:disposable@127.0.0.1:55432/ums` (or as confirmed at run time)
- Planned validation: Option B `downgrade base → upgrade head`
- `alembic upgrade head`: passed on disposable PostgreSQL; all migrations applied and converged to single head `20260521_0001`.
- Merge migration no-op check: passed; `upgrade()` and `downgrade()` are pure `pass`.
- Option B round-trip: blocked by pre-existing irreversible migration `20260513_0002`.
  The migration intentionally deletes retired graph-read scopes and grants during graph retirement. Option A would hit
  the same irreversible downgrade edge, so this is recorded as a main-branch carry-over rather than an integration
  regression.

### 3.6 Validation gate (ruff, pytest, frontend build, git diff --check)

- `python -m ruff check backend tests`: one pre-existing `E501` in `20260513_0003`; the migration blob is identical to
  `origin/main` and not changed by this integration.
- Focused Ruff after test updates:
  `python -m ruff check tests/connectors/test_credentials.py tests/api/test_finance_close_api.py tests/reports/test_raw_files.py tests/org/test_sql_channel_registry.py`
  passed.
- `git diff --check`: passed; Windows line-ending normalization warnings only.
- `source ~/.bashrc && cd frontend && npm install && npm run build`: passed on Node 22 LTS; Vite built successfully.
- Initial `pytest -q`: failed with 16 stack-added tests asserting pre-merge contracts.
- Focused post-fix rerun:
  `python -m pytest -q tests/connectors/test_credentials.py tests/api/test_finance_close_api.py tests/reports/test_raw_files.py tests/org/test_sql_channel_registry.py`
  passed: 114 passed.
- Final full `pytest -q`: passed, 734 passed / 7 SQLAlchemy reflection warnings.

### 3.7 Push + PR creation

- Push: `git push -u origin pr/s2-integration-merge` (2026-05-21 16:08:15Z) → branch created at `e308539` with upstream set.
- PR open: `gh pr create --base main --head pr/s2-integration-merge` → **PR #36** (<https://github.com/XGenerationy/Youtube/pull/36>) opened with operator-provided body, no Claude Code footer.
- Reviewer trigger comment posted (`@coderabbitai review` + `@codex review`, comment ID 4510177593).
- Initial PR state: `OPEN` / `MERGEABLE` / `BLOCKED` / `REVIEW_REQUIRED`; CodeRabbit status `PENDING` at 16:08:15Z.

### 3.8 Review-fix iteration + final validation against new tip

7 operator-pushed fixes responding to CodeRabbit + Codex findings (all authored as
`XGenerationy <redacted-email>`):

| Commit | Subject | Scope |
|---|---|---|
| `11868a0` | fix(tenant): resolve PR 36 review blockers | broad — auth, connectors, exports, finance, reports + repository tests (31 files). Also closes the prior runlog's queued follow-up by adding `tenant_id` filter to `_get_row` / `_get_row_for_update` in `reports/exports.py` |
| `f4db8ff` | docs: tenant-scope number explanations table | `Docs/13_SQL_DATA_MODEL.md` |
| `2e177b0` | fix(auth): bind principals to stored tenant | `auth/principals.py` + tests |
| `d14bb55` | docs: clarify tenant model notes | docs + tests |
| `a651d38` | fix(auth): require request tenant context | `api/dependencies.py`, `app.py`, `auth/users.py`, `org/access_index.py` + tests |
| `f687390` | fix(tenancy): harden resolver review findings | `app.py`, `tenancy/resolver.py` + tests |
| `9e49100` | fix(tenancy): hold resolver admission until worker completion | `tenancy/resolver.py` + tests — closes CR's `CHANGES_REQUESTED` against `f687390` |

Final review verdict:

- **CodeRabbit status check** on `9e49100`: `SUCCESS` (2026-05-21 19:58:20Z).
- **CodeRabbit formal review state**: last formal review was `CHANGES_REQUESTED` against `f687390` (19:24:33Z) — `future.cancel()` on `asyncio.wrap_future(run_in_executor(...))` released the admission semaphore while the underlying executor thread kept running. Commit `9e49100` swaps `loop.run_in_executor(None, …)` for a dedicated `ThreadPoolExecutor(max_workers=blocking_task_limit, thread_name_prefix="ums-tenant-resolver")`, attaches the `add_done_callback` semaphore-release to the real `concurrent.futures.Future` via `loop.call_soon_threadsafe`, wraps the worker future with `asyncio.wrap_future(worker_future, loop=loop)` for `asyncio.wait_for`, and drops both `future.cancel()` calls. `weakref.finalize(self, self._blocking_executor.shutdown, wait=False, cancel_futures=True)` registers a teardown hook; an explicit `close()` exposes the same path. Inverted test `test_blocking_timeout_keeps_slot_until_worker_finishes` proves the new contract: a second timed-out call cannot claim the slot until the first worker thread exits.
- **Codex** last PR-level comment (19:54:54Z): "Didn't find any major issues. Swish!"
- The PR-level `reviewDecision: CHANGES_REQUESTED` is a stale GitHub artifact of CR's `f687390` review — GitHub does not auto-clear a `CHANGES_REQUESTED` decision when subsequent commits address the finding via status check alone. Merge will need either a manual CR review dismissal on the GitHub UI or `gh pr merge --merge --admin` to bypass the stale-decision branch protection rule.

Local re-validation after `git pull --ff-only` to `9e49100` (2026-05-21, this session):

- `python -m ruff check backend tests` → **All checks passed!**
- `python -m pytest -q` → **756 passed** in 90.47s, 7 pre-existing `SAWarning: Skipped unsupported reflection of expression-based index uq_users_email_lower` carried over from §3.6.
- `git diff --check` → clean (no whitespace / conflict markers).
- HEAD: `9e49100ad62d0e14f81753b85c9d24d16f186039`.

Working-tree carryover (intentional, **not** part of PR diff):

- `M frontend/package-lock.json` (Node 22 LTS install side-effect; carried since §3.6).
- Untracked: `.agents/`, `AGENTS.md`, `Docs/superpowers/runlog/2026-05-21-phase-0.md`, `backend/ums_smart_revenue/devtools/`, `frontend/.vite/`, `scripts/`, `skills-lock.json`, `tests/devtools/`.

Merge completed: **PR #36** merged into `main` at `2026-05-21T22:04:00Z` by `XGenerationy`; merge commit `96dbe73dcd8baf571b752f485a2deb58d55d8bcf`.

### 3.9 Rollback information (anchored after merge)

- `INTEGRATION_MERGE_SHA`: `96dbe73dcd8baf571b752f485a2deb58d55d8bcf`
- `LAST_PRE_MERGE_HEAD`: `20260518_0001` (per decision 3 above)

---

## 4. Execution timeline

| Timestamp (UTC) | Step | Result |
|---|---|---|
| 2026-05-21 (prereq verification) | Read-only state capture | Recorded in §1 |
| 2026-05-21 (branch cut) | `git checkout -b pr/s2-integration-merge origin/main` | _(filled in immediately below)_ |
| 2026-05-21 (validation) | Disposable PostgreSQL Alembic upgrade | Passed; single head `20260521_0001` |
| 2026-05-21 (validation) | Alembic Option B round-trip | Blocked by pre-existing irreversible graph-retirement migration `20260513_0002` |
| 2026-05-21 (test fix) | Updated failing stack-added tests to settled integrated contracts | Focused rerun passed: 114 passed |
| 2026-05-21 16:08:15Z | `git push -u origin pr/s2-integration-merge` | Branch created at `e308539`; upstream set |
| 2026-05-21 16:08:15Z | `gh pr create --base main --head pr/s2-integration-merge` | PR #36 opened (OPEN / MERGEABLE / BLOCKED / REVIEW_REQUIRED) |
| 2026-05-21 16:15–19:54Z | CR + Codex review loop on `e308539..9e49100` (7 operator-pushed fixes) | CR final status check on `9e49100` SUCCESS; Codex "no major issues" |
| 2026-05-21 (this session) | `git pull --ff-only` to `9e49100`, then ruff + pytest + `git diff --check` | ruff clean / 756 passed / diff clean — merge readiness confirmed locally |
| 2026-05-21 22:04:00Z | PR #36 merged into `main` | Merge commit `96dbe73dcd8baf571b752f485a2deb58d55d8bcf`; head `9e49100ad62d0e14f81753b85c9d24d16f186039` |
